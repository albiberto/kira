<p style="color: lightgrey; font-size: 24px; text-align: center; margin: 2rem;">@(ChildContent is null ? "No records to display." : ChildContent)</p>

@code {
    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}